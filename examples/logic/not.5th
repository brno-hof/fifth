push 1
not
print_line
push 0
not
print_line
halt

# n -> !n
not:
  if # n = true (<=> n > 0)
    pop
    push 0
  else # n = false (<=> n > 0)
    pop
    push 1
  then
return

print_line:
  dup
  print_byte
  push 10
  print_char
  return
